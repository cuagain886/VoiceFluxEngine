// Package adapter defines the pluggable streaming interfaces for ASR, LLM and
// TTS plus a deterministic mock implementation (M4). Real adapters are cloud
// streaming APIs first, with a local stack as a later pluggable option.
package adapter
